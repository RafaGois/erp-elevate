/**
 * Interface para compatibilidade com a API de tipos de movimentação (se ainda existir).
 * Para novos fluxos, use o enum MovimentationType.
 */
export default interface Type {
  id: string;
  name: string;
}
